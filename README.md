# Webmotors Studio

Aplicação de estúdio virtual para fotos de veículos.

## Como rodar localmente

```bash
npm install
REMOVE_BG_API_KEY=sua_chave node server.js
```

## Variáveis de ambiente

- `REMOVE_BG_API_KEY` — chave da API Remove.bg
- `PORT` — porta do servidor (padrão: 3000)
